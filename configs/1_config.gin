# config gin parameters
# ----------------------------------------------------------------------------
# ------------------------- 01- DatasetsCollection class parameter ------------------------
# ----------------------------------------------------------------------------
DatasetsCollection.dir_data = './data'
DatasetsCollection.attributes = { 'non-contrast-data': {'path': ['non-contrast-data'],
                                              'type': ['CT'],
                                              'class': 'NonContrastDataLoader'
                                              },
                        'iDB-CERMEP': {'path': ['iDB-CERMEP/derivatives/coregistration'],
                                              'type': ['CT','FLAIR', 'T1W'],
                                              'class': 'iDBCERMEPLoader'
                                              }
                        }
# ----------------------------------------------------------------------------
# ------------------------- 01- main parameter ------------------------
# ----------------------------------------------------------------------------
main.operation = 'EDA' # 'EDA' or 'data_creation' or 'train' or 'test'